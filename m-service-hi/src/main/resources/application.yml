server:
  port: 4764
spring:
  application:
    name: service-hi
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:4761/eureka