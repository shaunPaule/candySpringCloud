server:
  port: 4765
spring:
  application:
    name: service-ribbon
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:4761/eureka